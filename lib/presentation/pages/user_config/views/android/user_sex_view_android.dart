import 'package:flutter/material.dart';

class UserSexViewAndroid extends StatelessWidget {
  const UserSexViewAndroid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Sex view android');
  }
}
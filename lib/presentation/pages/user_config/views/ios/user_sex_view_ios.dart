import 'package:flutter/material.dart';

class UserSexViewIos extends StatelessWidget {
  const UserSexViewIos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('sex view ios');
  }
}